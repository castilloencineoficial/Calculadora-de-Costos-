<!doctype html>
<html lang="es" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>Calculadora de Costos</title>
  <meta name="description" content="Cotiza servicios de edición, motion graphics y postproducción en un flujo premium de tres pasos." />
  <link rel="preconnect" href="https://api.fontshare.com">
  <link href="https://api.fontshare.com/v2/css?f[]=satoshi@400,500,700,900&f[]=boska@500,700&display=swap" rel="stylesheet">
  <style>
    :root, [data-theme="light"] {
      --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
      --text-sm: clamp(0.875rem, 0.8rem + 0.35vw, 1rem);
      --text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
      --text-lg: clamp(1.125rem, 1rem + 0.75vw, 1.5rem);
      --text-xl: clamp(1.5rem, 1.2rem + 1.25vw, 2.25rem);
      --text-2xl: clamp(2rem, 1.2rem + 2.5vw, 3.8rem);
      --space-1: 0.25rem;
      --space-2: 0.5rem;
      --space-3: 0.75rem;
      --space-4: 1rem;
      --space-5: 1.25rem;
      --space-6: 1.5rem;
      --space-8: 2rem;
      --space-10: 2.5rem;
      --space-12: 3rem;
      --space-16: 4rem;
      --color-bg: #E6E6E6;
      --color-surface: #F2F2F2;
      --color-surface-2: #FAFAFA;
      --color-surface-offset: #D9D9D9;
      --color-border: #CFCFCF;
      --color-divider: #C9C9C9;
      --color-text: #0B0B0B;
      --color-text-muted: #3B3B3B;
      --color-text-faint: #666666;
      --color-text-inverse: #F7F7F7;
      --color-primary: #ED8002;
      --color-primary-hover: #C96B00;
      --color-primary-highlight: #FFE7CC;
      --color-secondary: #FF9A1F;
      --radius-sm: 0.375rem;
      --radius-md: 0.75rem;
      --radius-lg: 1rem;
      --radius-xl: 1.5rem;
      --radius-2xl: 2rem;
      --radius-full: 9999px;
      --shadow-sm: 0 1px 2px rgba(11,11,11,0.06);
      --shadow-md: 0 12px 32px rgba(11,11,11,0.08);
      --shadow-lg: 0 24px 50px rgba(11,11,11,0.12);
      --shadow-xl: 0 32px 80px rgba(11,11,11,0.14);
      --transition: 180ms cubic-bezier(0.16, 1, 0.3, 1);
      --font-display: 'Boska', Georgia, serif;
      --font-body: 'Satoshi', Inter, sans-serif;
    }

    [data-theme="dark"] {
      --color-bg: #0B0B0B;
      --color-surface: #131313;
      --color-surface-2: #1B1B1B;
      --color-surface-offset: #242424;
      --color-border: #303030;
      --color-divider: #2A2A2A;
      --color-text: #E6E6E6;
      --color-text-muted: #BFBFBF;
      --color-text-faint: #8F8F8F;
      --color-text-inverse: #0B0B0B;
      --color-primary: #ED8002;
      --color-primary-hover: #FF9A1F;
      --color-primary-highlight: #3A240A;
      --color-secondary: #FFAA47;
      --shadow-sm: 0 1px 2px rgba(0,0,0,0.28);
      --shadow-md: 0 12px 32px rgba(0,0,0,0.36);
      --shadow-lg: 0 24px 50px rgba(0,0,0,0.42);
      --shadow-xl: 0 32px 80px rgba(0,0,0,0.5);
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; -webkit-font-smoothing: antialiased; text-size-adjust: none; }
    body {
      min-height: 100dvh;
      font-family: var(--font-body);
      font-size: var(--text-base);
      line-height: 1.5;
      background:
        radial-gradient(circle at top left, color-mix(in srgb, var(--color-primary) 12%, transparent), transparent 28%),
        radial-gradient(circle at top right, color-mix(in srgb, var(--color-secondary) 10%, transparent), transparent 24%),
        linear-gradient(180deg, color-mix(in srgb, var(--color-surface) 65%, transparent), transparent),
        var(--color-bg);
      color: var(--color-text);
      padding: var(--space-4);
    }

    button, input, select, textarea { font: inherit; color: inherit; }
    button { cursor: pointer; border: none; background: none; }
    a { color: inherit; text-decoration: none; }
    :focus-visible { outline: 2px solid var(--color-primary); outline-offset: 3px; border-radius: var(--radius-sm); }

    .app { width: min(100%, 1160px); margin: 0 auto; }
    .topbar {
      display: flex; align-items: center; justify-content: space-between; gap: var(--space-4);
      margin-bottom: var(--space-6); padding-top: max(var(--space-2), env(safe-area-inset-top));
    }
    .brand { display: flex; align-items: center; gap: var(--space-3); }
    .logo {
      width: 48px; height: 48px; border-radius: 16px; background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
      display: grid; place-items: center; color: white; box-shadow: var(--shadow-sm); position: relative; overflow: hidden;
    }
    .logo::after {
      content: ''; position: absolute; inset: 1px; border-radius: 15px; border: 1px solid rgba(255,255,255,0.24);
    }
    .brand-copy small {
      display: block; font-size: var(--text-xs); text-transform: uppercase; letter-spacing: 0.16em; color: var(--color-text-muted); margin-bottom: 0.15rem;
    }
    .brand-copy strong { display: block; font-size: var(--text-sm); font-weight: 700; }

    .icon-btn, .btn {
      min-height: 48px; border-radius: var(--radius-full); display: inline-flex; align-items: center; justify-content: center; gap: var(--space-2);
      transition: transform var(--transition), background var(--transition), border-color var(--transition), opacity var(--transition), box-shadow var(--transition);
    }
    .icon-btn {
      min-width: 48px; background: var(--color-surface); border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); box-shadow: var(--shadow-sm);
    }
    .btn { padding: 0.95rem 1.25rem; font-weight: 700; }
    .btn:hover, .icon-btn:hover, .service-card:hover, .step-card:hover { transform: translateY(-1px); }
    .btn:active, .icon-btn:active, .service-card:active, .step-card:active { transform: scale(0.98); }
    .btn-primary {
      background: linear-gradient(135deg, var(--color-primary), var(--color-secondary)); color: white; box-shadow: var(--shadow-md);
    }
    .btn-primary:hover { box-shadow: var(--shadow-lg); }
    .btn-secondary {
      background: transparent; color: var(--color-text); border: 1px solid color-mix(in srgb, var(--color-text) 14%, transparent);
    }

    .hero {
      position: relative;
      overflow: hidden;
      background: linear-gradient(180deg, color-mix(in srgb, var(--color-primary) 7%, var(--color-surface-2)), var(--color-surface));
      border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
      border-radius: calc(var(--radius-2xl) + 0.2rem);
      padding: var(--space-8);
      box-shadow: var(--shadow-xl);
      margin-bottom: var(--space-5);
      isolation: isolate;
    }
    .hero::before {
      content: '';
      position: absolute;
      inset: auto -8% -25% auto;
      width: 340px;
      height: 340px;
      border-radius: 50%;
      background: radial-gradient(circle, color-mix(in srgb, var(--color-primary) 22%, transparent), transparent 70%);
      z-index: -1;
      pointer-events: none;
    }
    .pill {
      display: inline-flex; align-items: center; gap: var(--space-2); padding: 0.45rem 0.8rem; border-radius: var(--radius-full);
      background: var(--color-primary-highlight); color: var(--color-primary-hover); font-size: var(--text-xs); font-weight: 700;
      letter-spacing: 0.06em; text-transform: uppercase; margin-bottom: var(--space-4);
    }
    h1 {
      font-family: var(--font-display); font-size: var(--text-2xl); line-height: 0.92; letter-spacing: -0.03em; margin-bottom: var(--space-4); max-width: 11ch;
    }
    .hero p {
      color: var(--color-text-muted); max-width: 56ch; margin-bottom: var(--space-5);
    }
    .hero-row {
      display: flex; flex-wrap: wrap; gap: var(--space-3);
    }
    .hero-chip {
      display: inline-flex; align-items: center; gap: var(--space-2); padding: 0.8rem 1rem; border-radius: var(--radius-full);
      background: color-mix(in srgb, var(--color-surface-2) 86%, transparent); border: 1px solid color-mix(in srgb, var(--color-text) 8%, transparent);
      font-size: var(--text-sm); color: var(--color-text-muted);
    }

    .progress {
      display: grid; grid-template-columns: repeat(3, 1fr); gap: var(--space-3); margin-bottom: var(--space-5);
    }
    .step-card {
      background: var(--color-surface); border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); border-radius: var(--radius-xl);
      padding: var(--space-4); box-shadow: var(--shadow-sm); transition: border-color var(--transition), background var(--transition), transform var(--transition);
    }
    .step-card.active {
      border-color: color-mix(in srgb, var(--color-primary) 55%, transparent); background: color-mix(in srgb, var(--color-primary) 8%, var(--color-surface));
    }
    .step-card span {
      display: block; font-size: var(--text-xs); text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-text-muted); margin-bottom: var(--space-2);
    }
    .step-card strong { display: block; font-size: var(--text-sm); }

    .screen {
      display: none; background: var(--color-surface); border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent);
      border-radius: calc(var(--radius-2xl) + 0.1rem); box-shadow: var(--shadow-lg); overflow: hidden;
    }
    .screen.active { display: block; }
    .screen-head {
      display: flex; align-items: end; justify-content: space-between; gap: var(--space-4);
      padding: var(--space-5); border-bottom: 1px solid var(--color-divider);
    }
    .screen-head h2 { font-size: var(--text-lg); line-height: 1.15; }
    .screen-head p { font-size: var(--text-sm); color: var(--color-text-muted); max-width: 42ch; margin-top: 0.35rem; }
    .screen-body { padding: var(--space-5); display: grid; gap: var(--space-5); }

    .services-grid {
      display: grid; gap: var(--space-4); grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .service-card {
      text-align: left; padding: var(--space-5); border-radius: calc(var(--radius-xl) + 0.2rem); background: var(--color-surface-2);
      border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); transition: transform var(--transition), border-color var(--transition), background var(--transition), box-shadow var(--transition);
      min-height: 100%; box-shadow: var(--shadow-sm); position: relative; overflow: hidden;
    }
    .service-card::before {
      content: ''; position: absolute; inset: 0 auto auto 0; width: 100%; height: 4px; background: linear-gradient(90deg, var(--color-primary), var(--color-secondary)); opacity: 0;
      transition: opacity var(--transition);
    }
    .service-card:hover::before { opacity: 1; }
    .service-card:hover {
      border-color: color-mix(in srgb, var(--color-primary) 40%, transparent); box-shadow: var(--shadow-md);
    }
    .service-card small {
      display: block; font-size: var(--text-xs); text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-primary-hover); margin-bottom: var(--space-2); font-weight: 700;
    }
    .service-card h3 { font-size: var(--text-base); margin-bottom: var(--space-2); }
    .service-card p { font-size: var(--text-sm); color: var(--color-text-muted); margin-bottom: var(--space-4); max-width: 30ch; }
    .service-card strong { display: block; font-size: var(--text-lg); margin-bottom: var(--space-4); }

    .config-layout, .summary-layout {
      display: grid; gap: var(--space-5); grid-template-columns: minmax(0, 1.08fr) minmax(320px, 0.92fr);
    }
    .field-grid { display: grid; gap: var(--space-3); grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .field { display: grid; gap: var(--space-2); }
    .field.full { grid-column: 1 / -1; }
    .field label {
      font-size: var(--text-xs); text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-text-muted); font-weight: 700;
    }
    .field input, .field select, .field textarea {
      width: 100%; min-height: 52px; border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); background: var(--color-surface-2);
      color: var(--color-text); border-radius: var(--radius-lg); padding: 0.95rem 1rem;
    }
    .field textarea { min-height: 128px; resize: vertical; }

    .option-list { display: grid; gap: var(--space-3); }
    .option {
      display: flex; align-items: center; justify-content: space-between; gap: var(--space-4); padding: var(--space-4);
      background: var(--color-surface-2); border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); border-radius: var(--radius-xl);
      transition: border-color var(--transition), transform var(--transition), box-shadow var(--transition);
    }
    .option:hover {
      border-color: color-mix(in srgb, var(--color-primary) 28%, transparent); box-shadow: var(--shadow-sm);
    }
    .option-copy h3 { font-size: var(--text-sm); margin-bottom: 0.15rem; }
    .option-copy p { color: var(--color-text-muted); font-size: var(--text-xs); max-width: 34ch; }
    .toggle {
      position: relative; width: 54px; height: 32px; border-radius: var(--radius-full); background: var(--color-surface-offset);
      border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); flex-shrink: 0; transition: background var(--transition);
    }
    .toggle::after {
      content: ''; position: absolute; top: 3px; left: 3px; width: 24px; height: 24px; border-radius: 50%;
      background: var(--color-surface-2); box-shadow: var(--shadow-sm); transition: transform var(--transition), background var(--transition);
    }
    .option input:checked + .toggle { background: color-mix(in srgb, var(--color-primary) 30%, var(--color-primary-highlight)); }
    .option input:checked + .toggle::after { transform: translateX(22px); background: var(--color-primary); }

    .aside-card, .summary-card {
      background: linear-gradient(180deg, color-mix(in srgb, var(--color-primary) 7%, var(--color-surface)), var(--color-surface));
      border: 1px solid color-mix(in srgb, var(--color-text) 10%, transparent); border-radius: calc(var(--radius-2xl) - 0.15rem); padding: var(--space-5); box-shadow: var(--shadow-sm);
    }
    .aside-card h3, .summary-card h3 { font-size: var(--text-base); margin-bottom: var(--space-3); }
    .aside-card p, .summary-note { color: var(--color-text-muted); font-size: var(--text-sm); }
    .meta-list { display: grid; gap: var(--space-3); margin-top: var(--space-4); }
    .meta-item {
      display: flex; justify-content: space-between; gap: var(--space-3); padding-bottom: var(--space-3); border-bottom: 1px solid var(--color-divider); font-size: var(--text-sm);
    }
    .meta-item:last-child { border-bottom: 0; padding-bottom: 0; }
    .meta-item span:last-child { font-weight: 700; text-align: right; }

    .summary-price {
      display: block; font-size: clamp(2rem, 1.4rem + 2vw, 3rem); line-height: 1; margin: var(--space-2) 0 var(--space-4); font-variant-numeric: tabular-nums;
    }

    .value-boxes {
      display: grid; gap: var(--space-3); grid-template-columns: repeat(3, minmax(0, 1fr));
    }
    .value-box {
      padding: var(--space-4); border-radius: var(--radius-xl); background: color-mix(in srgb, var(--color-surface-2) 90%, transparent);
      border: 1px solid color-mix(in srgb, var(--color-text) 8%, transparent);
    }
    .value-box span {
      display: block; font-size: var(--text-xs); color: var(--color-text-muted); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: var(--space-2);
    }
    .value-box strong { display: block; font-size: var(--text-sm); }

    .nav-row { display: flex; justify-content: space-between; gap: var(--space-3); flex-wrap: wrap; }
    .nav-row.right { justify-content: flex-end; }

    .footer {
      padding: var(--space-8) 0 var(--space-6); color: var(--color-text-faint); font-size: var(--text-xs); text-align: center;
    }

    @media (max-width: 919px) {
      .progress, .services-grid, .config-layout, .summary-layout, .field-grid, .value-boxes { grid-template-columns: 1fr; }
      .field.full { grid-column: auto; }
      .screen-head { align-items: start; flex-direction: column; }
      .nav-row > * { width: 100%; }
      .btn { width: 100%; }
      .hero { padding: var(--space-6); }
    }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important;
      }
    }
  </style>
</head>
<body>
  <div class="app">
    <header class="topbar">
      <div class="brand">
        <div class="logo" aria-hidden="true">
          <img src="profile.png" alt="Foto de perfil del editor freelance" width="50" height="50" style="border-radius: 0%; object-fit: cover;">
            <path d="M5 17.5L12 5L19 17.5" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M8.8 13.2H15.2" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"/>
          </svg>
        </div>
        <div class="brand-copy">
          <small>Edición · Motion · Post</small>
          <strong>Tu editor freelance</strong>
        </div>
      </div>
      <button class="icon-btn" type="button" aria-label="Cambiar tema" data-theme-toggle>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M21 12.79A9 9 0 1 1 11.21 3A7 7 0 0 0 21 12.79Z"></path>
        </svg>
      </button>
    </header>

    <section class="hero">
      <div class="pill">Cotiza tu proyecto en 3 pasos</div>
      <h1>Para marcas que quieren verse mejor y comunicar con más impacto.</h1>
      <p>Trabajo contenido corto, videos largos, motion graphics y solicitudes especiales para proyectos que necesitan una ejecución cuidada, clara y visualmente fuerte. Elige el servicio, personaliza tu cotización y envíame tu solicitud con el resumen listo.</p>
      <div class="hero-row">
        <div class="hero-chip">Proceso claro</div>
        <div class="hero-chip">Precio estimado al instante</div>
        <div class="hero-chip">Contacto directo por WhatsApp</div>
      </div>
    </section>

    <section class="progress" aria-label="Progreso del flujo">
      <div class="step-card active" data-step-card="1"><span>Paso 1</span><strong>Selecciona el servicio</strong></div>
      <div class="step-card" data-step-card="2"><span>Paso 2</span><strong>Personaliza la cotización</strong></div>
      <div class="step-card" data-step-card="3"><span>Paso 3</span><strong>Revisa y contacta</strong></div>
    </section>

    <section class="screen active" id="screen-1">
      <div class="screen-head">
        <div>
          <h2>Servicios disponibles</h2>
          <p>Escoge la categoría que mejor se adapte a lo que necesitas. Cada opción tiene su propia lógica de duración, entrega y personalización.</p>
        </div>
      </div>
      <div class="screen-body">
        <div class="services-grid" id="serviceGrid"></div>
      </div>
    </section>

    <section class="screen" id="screen-2">
      <div class="screen-head">
        <div>
          <h2>Configura tu proyecto</h2>
          <p>Define la cantidad, duración, tiempo de entrega, revisiones y extras para recibir un estimado más preciso según el servicio seleccionado.</p>
        </div>
      </div>
      <div class="screen-body">
        <div class="config-layout">
          <div>
            <div class="field-grid" id="configFields"></div>
            <div class="option-list" id="addonsList"></div>
          </div>

          <aside class="aside-card">
            <h3 id="configServiceName">Servicio</h3>
            <p id="configServiceText">Selecciona una categoría para continuar.</p>
            <div class="meta-list" id="configMeta"></div>
          </aside>
        </div>

        <div class="nav-row">
          <button class="btn btn-secondary" type="button" id="backToServices">Volver</button>
          <div style="display:flex; gap:0.75rem; flex-wrap:wrap;">
            <button class="btn btn-secondary" type="button" id="resetButton">Reset</button>
            <button class="btn btn-primary" type="button" id="goToSummary">Continuar</button>
          </div>
        </div>
      </div>
    </section>

    <section class="screen" id="screen-3">
      <div class="screen-head">
        <div>
          <h2>Tu resumen</h2>
          <p>Revisa el estimado y envíame el brief por WhatsApp para continuar con una conversación mucho más rápida y precisa.</p>
        </div>
      </div>
      <div class="screen-body">
        <div class="value-boxes">
          <div class="value-box"><span>Respuesta</span><strong>Consulta más clara desde el inicio</strong></div>
          <div class="value-box"><span>Proceso</span><strong>Menos ida y vuelta al cotizar</strong></div>
          <div class="value-box"><span>Objetivo</span><strong>Avanzar con contexto real</strong></div>
        </div>

        <div class="summary-layout">
          <div class="summary-card">
            <h3 id="summaryService">Servicio seleccionado</h3>
            <span class="summary-price" id="summaryPrice">—</span>
            <div class="meta-list" id="summaryList"></div>
            <p class="summary-note" id="summaryNote">Completa el flujo para ver el resumen.</p>
          </div>

          <aside class="aside-card">
            <h3>Continúa por WhatsApp</h3>
            <p>Tu mensaje incluirá el servicio, duración, tiempo de entrega, cantidad de piezas, extras y los detalles escritos para que la conversación empiece con una base real.</p>
            <div class="nav-row right" style="margin-top:1.25rem;">
              <a class="btn btn-primary" id="whatsappButton" href="#" target="_blank" rel="noopener noreferrer">Enviar resumen</a>
              <button class="btn btn-secondary" type="button" id="copySummary">Copiar resumen</button>
              <button class="btn btn-secondary" type="button" id="backToConfig">Atrás</button>
            </div>
          </aside>
        </div>
      </div>
    </section>

    <footer class="footer">
      "La publicidad no es un gasto, es la inversión más rentable para tu empresa." — Anónimo
    </footer>
  </div>

  <script>
    const whatsappNumber = '584129582823';

    const services = [
      {
        id: 'short',
        name: 'Contenido corto',
        tag: 'Reels / Shorts',
        description: 'Edición de reels, shorts y piezas verticales para marcas, creadores y campañas que necesitan captar atención rápido.',
        priceLabel: 'Desde $45',
        basePrice: 45,
        baseDuration: 'Hasta 1 minuto',
        baseDelivery: '3 días por pieza',
        durations: [
          { value: '1m', label: 'Hasta 1 minuto', extra: 0 },
          { value: '2m', label: 'Hasta 2 minutos (+10 USD)', extra: 10 },
          { value: '3m', label: 'Hasta 3 minutos (+20 USD)', extra: 20 }
        ],
        turnarounds: [
          { value: 'normal', label: 'Normal — 3 días por pieza', multiplier: 1, note: '3 días por pieza' },
          { value: 'fast', label: 'Rápida — +35% · 48 horas por pieza', multiplier: 1.35, note: '48 horas por pieza' },
          { value: 'urgent', label: 'Urgente — +50% · 24 horas por pieza', multiplier: 1.5, note: '24 horas por pieza' }
        ],
        addons: [
          { id: 'subtitles', label: 'Subtítulos para redes', description: 'Mejora claridad, retención y comprensión.', price: 12 },
          { id: 'extraFormat', label: 'Formato adicional', description: 'Versión extra para otro formato o canal.', price: 15 },
          { id: 'thumbnail', label: 'Portada', description: 'Frame o portada para reforzar presentación.', price: 10 }
        ]
      },
      {
        id: 'long',
        name: 'Contenido largo',
        tag: 'YouTube / Podcast',
        description: 'Edición de videos largos para YouTube, entrevistas, podcasts y piezas que buscan reforzar autoridad y claridad.',
        priceLabel: 'Desde $120',
        basePrice: 120,
        baseDuration: 'Hasta 10 minutos',
        baseDelivery: '7 días por pieza',
        durations: [
          { value: '10m', label: 'Hasta 10 minutos', extra: 0 },
          { value: '20m', label: 'Hasta 20 minutos (+35 USD)', extra: 35 },
          { value: '30m', label: 'Hasta 30 minutos (+60 USD)', extra: 60 }
        ],
        turnarounds: [
          { value: 'normal', label: 'Normal — 7 días por pieza', multiplier: 1, note: '7 días por pieza' },
          { value: 'fast', label: 'Rápida — +35% · 5 días por pieza', multiplier: 1.35, note: '5 días por pieza' },
          { value: 'urgent', label: 'Urgente — +50% · 3 días por pieza', multiplier: 1.5, note: '3 días por pieza' }
        ],
        addons: [
          { id: 'subtitles', label: 'Subtítulos completos', description: 'Más claridad y mejor reutilización del contenido.', price: 22 },
          { id: 'audio', label: 'Limpieza de audio', description: 'Reduce ruido y mejora percepción general.', price: 18 },
          { id: 'thumbnail', label: 'Miniatura YouTube', description: 'Diseño orientado a una mejor presentación.', price: 18 },
          { id: 'clips', label: '2 clips promocionales', description: 'Recortes para distribución en redes.', price: 30 }
        ]
      },
      {
        id: 'motion',
        name: 'Motion graphics',
        tag: 'Animación',
        description: 'Motion graphics para anuncios, lanzamientos y piezas visuales con una ejecución más premium y controlada.',
        priceLabel: 'Desde $100',
        basePrice: 100,
        baseDuration: 'Hasta 15 segundos',
        baseDelivery: '5 días por pieza',
        durations: [
          { value: '15s', label: 'Hasta 15 segundos', extra: 0 },
          { value: '30s', label: 'Hasta 30 segundos (+25 USD)', extra: 25 },
          { value: '45s', label: 'Hasta 45 segundos (+45 USD)', extra: 45 }
        ],
        turnarounds: [
          { value: 'normal', label: 'Normal — 5 días por pieza', multiplier: 1, note: '5 días por pieza' },
          { value: 'fast', label: 'Rápida — +35% · 3 días por pieza', multiplier: 1.35, note: '3 días por pieza' },
          { value: 'urgent', label: 'Urgente — +50% · 48 horas por pieza', multiplier: 1.5, note: '48 horas por pieza' }
        ],
        addons: [
          { id: 'sound', label: 'Diseño sonoro básico', description: 'Agrega presencia y mejor acabado final.', price: 20 },
          { id: 'versions', label: '2 versiones del concepto', description: 'Variaciones para testing o adaptaciones.', price: 35 },
          { id: 'adaptation', label: 'Adaptación a otro formato', description: 'Ajuste para otra salida o pantalla.', price: 25 }
        ]
      },
      {
        id: 'other',
        name: 'Otro servicio',
        tag: 'Consulta especial',
        description: 'Solicitudes especiales como animación 3D para redes o pantallas LED, VFX, composición avanzada o proyectos para empresas y startups.',
        priceLabel: 'Consulta directa',
        custom: true
      }
    ];

    const revisionFees = {
      1: { label: '1 revisión', price: 0 },
      2: { label: '2 revisiones', price: 10 },
      3: { label: '3 revisiones', price: 20 }
    };

    const state = { step: 1, selectedService: null, summaryText: '' };
    const serviceGrid = document.getElementById('serviceGrid');
    const screens = { 1: document.getElementById('screen-1'), 2: document.getElementById('screen-2'), 3: document.getElementById('screen-3') };
    const stepCards = document.querySelectorAll('[data-step-card]');
    const configFields = document.getElementById('configFields');
    const addonsList = document.getElementById('addonsList');
    const configServiceName = document.getElementById('configServiceName');
    const configServiceText = document.getElementById('configServiceText');
    const configMeta = document.getElementById('configMeta');
    const summaryService = document.getElementById('summaryService');
    const summaryPrice = document.getElementById('summaryPrice');
    const summaryList = document.getElementById('summaryList');
    const summaryNote = document.getElementById('summaryNote');
    const whatsappButton = document.getElementById('whatsappButton');
    const copySummary = document.getElementById('copySummary');
    const resetButton = document.getElementById('resetButton');
    const backToServices = document.getElementById('backToServices');
    const backToConfig = document.getElementById('backToConfig');
    const goToSummary = document.getElementById('goToSummary');
    const root = document.documentElement;
    const themeToggle = document.querySelector('[data-theme-toggle]');

    function money(value) {
      return new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD', maximumFractionDigits: 0 }).format(value);
    }

    function getService() {
      return services.find(service => service.id === state.selectedService) || null;
    }

    function setStep(step) {
      state.step = step;
      Object.entries(screens).forEach(([key, screen]) => screen.classList.toggle('active', Number(key) === step));
      stepCards.forEach(card => card.classList.toggle('active', Number(card.dataset.stepCard) === step));
      window.scrollTo({ top: 400, behavior: 'smooth' });
    }

    function renderServices() {
      serviceGrid.innerHTML = services.map(service => `
        <button class="service-card" type="button" data-service="${service.id}">
          <small>${service.tag}</small>
          <h3>${service.name}</h3>
          <p>${service.description}</p>
          <strong>${service.priceLabel}</strong>
          <span class="btn btn-primary" style="width:100%;">Seleccionar</span>
        </button>
      `).join('');

      serviceGrid.querySelectorAll('[data-service]').forEach(button => {
        button.addEventListener('click', () => {
          state.selectedService = button.dataset.service;
          renderConfig();
          setStep(2);
        });
      });
    }

    function renderConfig() {
      const service = getService();
      if (!service) return;

      configServiceName.textContent = service.name;
      configServiceText.textContent = service.custom
        ? 'Cuéntame qué necesitas y te responderé con una propuesta alineada al alcance real del proyecto.'
        : `Punto de partida ${service.priceLabel.toLowerCase()} · duración base ${service.baseDuration.toLowerCase()} · entrega estándar ${service.baseDelivery}.`;

      configMeta.innerHTML = service.custom
        ? `
          <div class="meta-item"><span>Tipo de solicitud</span><span>Consulta personalizada</span></div>
          <div class="meta-item"><span>Ideal para</span><span>3D, VFX, composición y proyectos de mayor escala</span></div>
        `
        : `
          <div class="meta-item"><span>Precio base</span><span>${money(service.basePrice)}</span></div>
          <div class="meta-item"><span>Duración base</span><span>${service.baseDuration}</span></div>
          <div class="meta-item"><span>Entrega base</span><span>${service.baseDelivery}</span></div>
        `;

      if (service.custom) {
        configFields.innerHTML = `
          <div class="field"><label for="customName">Nombre o empresa</label><input id="customName" type="text" placeholder="Ej. Startup X"></div>
          <div class="field"><label for="customBudget">Presupuesto estimado</label><input id="customBudget" type="text" placeholder="Opcional, ej. 800 USD"></div>
          <div class="field full"><label for="customBrief">Cuéntame sobre tu proyecto</label><textarea id="customBrief" placeholder="Ej. Necesito una animación 3D para pantallas LED o una edición con VFX y composición avanzada."></textarea></div>
        `;
        addonsList.innerHTML = '';
        return;
      }

      configFields.innerHTML = `
        <div class="field"><label for="quantity">Cantidad de piezas</label><input id="quantity" type="number" min="1" max="30" value="1" inputmode="numeric"></div>
        <div class="field"><label for="duration">Duración del video</label><select id="duration">${service.durations.map(item => `<option value="${item.value}">${item.label}</option>`).join('')}</select></div>
        <div class="field"><label for="turnaround">Tiempo de entrega</label><select id="turnaround">${service.turnarounds.map(item => `<option value="${item.value}">${item.label}</option>`).join('')}</select></div>
        <div class="field"><label for="revisions">Rondas de revisión</label><select id="revisions"><option value="1">1 revisión incluida</option><option value="2">2 revisiones (+10 USD)</option><option value="3">3 revisiones (+20 USD)</option></select></div>
        <div class="field"><label for="contactName">Nombre o marca</label><input id="contactName" type="text" placeholder="Ej. Studio Nova"></div>
        <div class="field full"><label for="projectNote">Detalles del proyecto</label><textarea id="projectNote" placeholder="Ej. Busco un estilo premium, ritmo dinámico y piezas pensadas para captar atención rápido."></textarea></div>
      `;

      addonsList.innerHTML = service.addons.map(addon => `
        <label class="option">
          <div class="option-copy">
            <h3>${addon.label}</h3>
            <p>${addon.description} · ${money(addon.price)} por pieza</p>
          </div>
          <div>
            <input class="sr-only addon" type="checkbox" value="${addon.id}">
            <span class="toggle" aria-hidden="true"></span>
          </div>
        </label>
      `).join('');
    }

    function buildSummary() {
      const service = getService();
      if (!service) return false;

      if (service.custom) {
        const customName = document.getElementById('customName')?.value.trim() || '';
        const customBudget = document.getElementById('customBudget')?.value.trim() || '';
        const customBrief = document.getElementById('customBrief')?.value.trim() || '';
        summaryService.textContent = 'Otro servicio';
        summaryPrice.textContent = 'A definir';
        summaryList.innerHTML = `
          <div class="meta-item"><span>Tipo</span><span>Consulta especial</span></div>
          <div class="meta-item"><span>Presupuesto</span><span>${customBudget || 'No indicado'}</span></div>
          <div class="meta-item"><span>Estado</span><span>${customBrief ? 'Brief agregado' : 'Pendiente de detalle'}</span></div>
        `;
        summaryNote.textContent = 'Esta consulta está pensada para proyectos especiales que requieren una propuesta a medida.';
        state.summaryText = `${customName ? `Hola, soy ${customName}.` : 'Hola.'}\nNecesito cotizar otro servicio.\n${customBudget ? `Presupuesto estimado: ${customBudget}.\n` : ''}${customBrief ? `Lo que necesito: ${customBrief}` : 'Quiero compartir los detalles del proyecto por aquí.'}`;
        whatsappButton.href = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(state.summaryText)}`;
        return true;
      }

      const quantity = Math.max(1, Number(document.getElementById('quantity')?.value) || 1);
      const duration = service.durations.find(item => item.value === document.getElementById('duration')?.value) || service.durations[0];
      const turnaround = service.turnarounds.find(item => item.value === document.getElementById('turnaround')?.value) || service.turnarounds[0];
      const revisions = revisionFees[document.getElementById('revisions')?.value || '1'];
      const name = document.getElementById('contactName')?.value.trim() || '';
      const note = document.getElementById('projectNote')?.value.trim() || '';
      const selectedAddons = Array.from(document.querySelectorAll('.addon:checked')).map(input => service.addons.find(addon => addon.id === input.value)).filter(Boolean);

      const baseSubtotal = service.basePrice * quantity;
      const durationTotal = duration.extra * quantity;
      const addonsTotal = selectedAddons.reduce((sum, addon) => sum + addon.price * quantity, 0);
      const revisionsTotal = revisions.price;
      const subtotal = baseSubtotal + durationTotal + addonsTotal + revisionsTotal;
      const rushFee = Math.round(subtotal * (turnaround.multiplier - 1));
      const total = subtotal + rushFee;

      summaryService.textContent = service.name;
      summaryPrice.textContent = money(total);

      const rows = [
        { label: `${service.name} × ${quantity}`, value: money(baseSubtotal) },
        { label: `Duración: ${duration.label}`, value: durationTotal ? money(durationTotal) : 'Incluida' },
        ...selectedAddons.map(addon => ({ label: `${addon.label} × ${quantity}`, value: money(addon.price * quantity) })),
        { label: revisions.label, value: revisionsTotal ? money(revisionsTotal) : 'Incluida' },
        { label: turnaround.label, value: rushFee ? money(rushFee) : 'Sin recargo' }
      ];

      summaryList.innerHTML = rows.map(row => `<div class="meta-item"><span>${row.label}</span><span>${row.value}</span></div>`).join('');
      summaryNote.textContent = `La base incluye ${service.baseDuration.toLowerCase()} y una entrega estándar de ${service.baseDelivery}.`;
      const addonsText = selectedAddons.length ? selectedAddons.map(addon => addon.label).join(', ') : 'Sin extras';
      state.summaryText = `${name ? `Hola, soy ${name}.` : 'Hola.'}\nQuiero cotizar el servicio: ${service.name}.\nCantidad: ${quantity}.\nDuración elegida: ${duration.label}.\nEntrega: ${turnaround.note}.\nExtras: ${addonsText}.\n${revisions.label}.\nEstimado mostrado en la web: ${money(total)}.\n${note ? `Detalles: ${note}` : 'Comparto más detalles por aquí.'}`;
      whatsappButton.href = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(state.summaryText)}`;
      return true;
    }

    function resetFlow() {
      state.selectedService = null;
      state.summaryText = '';
      configFields.innerHTML = '';
      addonsList.innerHTML = '';
      configServiceName.textContent = 'Servicio';
      configServiceText.textContent = 'Selecciona una categoría para continuar.';
      configMeta.innerHTML = '';
      summaryService.textContent = 'Servicio seleccionado';
      summaryPrice.textContent = '—';
      summaryList.innerHTML = '';
      summaryNote.textContent = 'Completa el flujo para ver el resumen.';
      whatsappButton.href = '#';
      setStep(1);
    }

    function setupTheme() {
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      let theme = prefersDark ? 'dark' : 'light';
      root.setAttribute('data-theme', theme);
      const updateIcon = () => {
        themeToggle.innerHTML = theme === 'dark'
          ? '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="5"></circle><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"></path></svg>'
          : '<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M21 12.79A9 9 0 1 1 11.21 3A7 7 0 0 0 21 12.79Z"></path></svg>';
      };
      updateIcon();
      themeToggle.addEventListener('click', () => {
        theme = theme === 'dark' ? 'light' : 'dark';
        root.setAttribute('data-theme', theme);
        updateIcon();
      });
    }

    renderServices();
    setupTheme();
    backToServices.addEventListener('click', () => setStep(1));
    backToConfig.addEventListener('click', () => setStep(2));
    resetButton.addEventListener('click', resetFlow);
    goToSummary.addEventListener('click', () => {
      if (!state.selectedService) return;
      const ready = buildSummary();
      if (ready) setStep(3);
    });
    copySummary.addEventListener('click', async () => {
      if (!state.summaryText) return;
      try {
        await navigator.clipboard.writeText(state.summaryText);
        copySummary.textContent = 'Resumen copiado';
        setTimeout(() => copySummary.textContent = 'Copiar resumen', 1800);
      } catch (error) {
        copySummary.textContent = 'No se pudo copiar';
        setTimeout(() => copySummary.textContent = 'Copiar resumen', 1800);
      }
    });
  </script>
</body>
</html>
